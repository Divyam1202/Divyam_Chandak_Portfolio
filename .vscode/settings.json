{
  // Ensure the ESLint extension resolves the workspace's node_modules
  "eslint.packageManager": "npm",
  "eslint.nodePath": "${workspaceFolder}/node_modules",
  "eslint.workingDirectories": [
    { "mode": "auto" }
  ]
}
